---
title: "Hello World"
date: "2026-08-07"
excerpt: "First post — pipeline test."
tags: ["meta"]
---

This is a test post. Write in markdown, `git push`, it publishes.

## Code blocks work

```python
def scan(host):
    pass
```

That's it. No CMS, no database, no build step beyond `next build`.
